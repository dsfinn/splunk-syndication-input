[launcher]
version = 1.2.6unauthenticated
description = Import syndication feeds into Splunk
author = LukeMurphey

[package]
id = syndication-unauthenticated

[install]
build = ${value.build.number}
# Last commit: ${value.build.date}

[ui]
is_visible = false
label = Syndication (RSS, ATOM, RDF) (Unauthenticated)